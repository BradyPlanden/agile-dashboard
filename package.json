{
  "name": "agile-dashboard-lint",
  "version": "0.5.2",
  "private": true,
  "description": "Linting tools for agile-dashboard CSS and HTML",
  "scripts": {
    "lint:css": "stylelint 'src/**/*.css'",
    "lint:css:fix": "stylelint 'src/**/*.css' --fix",
    "lint": "npm run lint:css"
  },
  "devDependencies": {
    "stylelint": "^16.12.0",
    "stylelint-config-standard": "^36.0.1"
  }
}
